<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Login</title>
  </head>
  <body>
    <header>
      <h1>Log In</h1>
      <a href="/">home</a>
    </header>

    <ul class="error">
      {{#errors}}
       <li>{{msg}}</li>
       {{/errors}}
   </ul>

    <section>
      <form class="" action='/' method="post">

          <input type="text" name="" value="{{type}}" placeholder="Username">

          <input type="text" name="" value="{{type}}" placeholder="Password">

        <button type="submit" name="button">Login</button>
      </form>

    </section>
  </body>
</html>
